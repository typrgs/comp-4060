# Assignment 2
Authored by Ty Paragas
Submitted for January 30th, 2026

## Compiling and running
Navigate to /assignments/A2 and run `make debug` to compile all necessary files. Flash the display and sensor units using `make display-install` and `make sensor-install` respectively.

Makefiles assume the directories `/Core/Include` and `/E51-pack/include` exist outside of the assignments directory.
